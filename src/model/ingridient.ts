export class Ingridient {
  name: string;
  category: string;
  unit; string;
  amount: string;
  constructor(mName, mCategory, mUnit) {
    this.name = name;
    this.category = mCategory;
    this.unit = mUnit;
  }
}
